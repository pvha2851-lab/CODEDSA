#include<bits/stdc++.h>
using namespace std;
int binarysearch(int l,int r,int a[],int x){
	while(l<=r){
		int mid=(l+r)/2;
		if(a[mid]==x){
			return mid+1;
		}
		else if(a[mid]<x){
			l=mid+1;
		}	
		else{
			r=mid-1;
		}
	}
	return -1;
}
int main(){
	int t;
	cin>>t;
	while(t--){
		int n,x;
		cin>>n>>x;
		int a[n];
		for(int i=0;i<n;i++){
			cin>>a[i];
		}
		sort(a,a+n);
		int res=binarysearch(0,n-1,a,x);
		if(res!=-1){
			cout<<res<<endl;
		}
		else{
			cout<<"NO\n";
		}
	}
	return 0;
}
