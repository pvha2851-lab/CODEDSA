#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	int t;
	cin>>t;
	cin.ignore();
	while(t--){
		string s;
		cin>>s;
		ll f=s[0]-'0';
		ll res=f;
		for(int i=1;i<s.size();i++){
			f=f*10+(i+1)*(s[i]-'0');
			res+=f;
		}
		cout<<res<<endl;
	}
	return 0;
}
