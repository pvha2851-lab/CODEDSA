#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	int t;
	cin>>t;
	cin.ignore();
	while(t--){
		string s;
		cin>>s;
		ll res=s[0]-'0';
		ll f=res;
		for(int i=1;i<s.size();i++){
			ll d=s[i]-'0';
			f=f*10+(i+1)*d;
			res+=f;
		}
		cout<<res<<endl;
	}
	return 0;
}
